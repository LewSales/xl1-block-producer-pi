@echo off
REM Edit these two lines if your Pi is named or reachable differently.
REM If xl1pi.local does not resolve, put the Pi's IP address here instead.
set PI_HOST=xl1pi.local
set PI_USER=pi
set DASH_PORT=8088
